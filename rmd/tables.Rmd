---
title: "Tables"
output: pdf_document
knit: (function(inputFile, encoding) {
        rmarkdown::render(inputFile, encoding = encoding, output_dir = "document_output")
      })
---

```{r table_param, echo = FALSE}

knitr::opts_chunk$set(echo = FALSE,
                      message = FALSE)

options(knitr.kable.NA = '')

source(here::here("code/table_param.R"))
knitr::kable(df_value,
             format = "markdown",
             caption = "Parameter values used for the main and sensitivity analysis.")

```
